/** GitHub repository that publishes Stitch releases. Keep in sync with `build.publish` in package.json. */
export const RELEASE_REPO = 'Darker117/stitch'
export const RELEASES_URL = `https://github.com/${RELEASE_REPO}/releases`
